import Canvas from "@/pages/api/mongodb/models/Canvas.js"

export default async function handler(req,res){
    if(req.method === 'POST'){
try {
    const {node} = req.body;
} catch (error) {
    
}
    }

}